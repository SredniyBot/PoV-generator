kind: task_template
id: implementation.prepare_build_plan
version: 1.0.0
title: Собрать план реализации
type: composite
status: active
domain: implementation
children:
  # Ф6: ось веера — компонент. На каждый компонент модели — своя спека сборки
  # (harness-вход), вместо пяти ролевых спек. Сводка по умениям — в синтезе.
  - id: component_build_fanout
    task: implementation.component_build_fanout@1.0.0
  - id: build_plan_synthesis
    task: implementation.build_plan_synthesis@1.0.0
# Ролевые спеки (backend/ui/ml/data/integration_build_spec) выведены из активного
# графа в пользу веера по компонентам; шаблоны сохранены для закреплённых
# графом прошлых проектов.
