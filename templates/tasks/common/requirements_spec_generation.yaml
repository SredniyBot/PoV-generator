kind: task_template
id: common.requirements_spec_generation
version: 1.0.0
title: Подготовить структурированное ТЗ
type: leaf
status: active
# Класс задачи для маршрутизации модели (execution.complex): финальный synthesis
# всего ТЗ из upstream-артефактов — качество критично, гонится на сильной модели.
complexity: complex
domain: common
executor: llm
# Финальный synthesis: option_generation/decision не применимы — выбор уже
# сделан в upstream.
methodology: minimal
# v3.6: финальный synthesis-merge не порождает новых проектных решений
# (все они уже в upstream-артефактах и в реестре). Identification бы
# дублировал что-то про оформление документа. Post-artifact extraction
# при этом ВКЛЮЧЕНА на уровне сервиса — она вытащит имплицитные тех-
# выборы, которые попали в финальное ТЗ.
decision_identification: false
# Этап 5: финальный ТЗ — синтетический merge всех аналитических артефактов.
# LLM интегрирует пересекающиеся темы (scope/stakeholders/options/...)
# в связный документ. Strategy=synthetic не меняет путь исполнения,
# но фиксирует семантику в метаинформации артефакта (для UI/provenance)
# и валидаторов merge-инвариантов.
merge:
  strategy: synthetic
  conflict_policy: union
requires:
  state: [business_request]
  artifacts:
    required:
      - common.normalized_request@1.0.0
      - common.business_outcome_model@1.0.0
      - common.scope_boundary_matrix@1.0.0
      - common.stakeholder_map@1.0.0
      - common.solution_option_inventory@1.0.0
      # Критерии приёмки и НФТ — ядро профессионального ТЗ, обязательны.
      - common.acceptance_model_definition@1.0.0
      - common.nonfunctional_requirements@1.0.0
    # Дополнительные аналитические артефакты, обогащающие финальный
    # documenting-проход. Они опциональны, потому что для совсем простых
    # PoV (тривиальная сложность) могут пропускаться. Если присутствуют —
    # финальный merge берёт их разделы.
    optional:
      - common.problem_statement@1.0.0
      - common.constraint_inventory@1.0.0
      - common.integration_data_map@1.0.0
      - common.solution_tradeoff_matrix@1.0.0
      - common.implementation_dependency_plan@1.0.0
      - common.delivery_scope_definition@1.0.0
      - common.glossary_terms@1.0.0
      - common.project_risk_register@1.0.0
      - common.deployment_topology@1.0.0
      - common.feasibility_assessment@1.0.0
      - security.privacy_impact_assessment@1.0.0
    # Этап 7.3: вместо hand-coded списка optional всех доменных артефактов
    # — декларативный auto-collect. Любая задача активного доменного
    # пака (origin_kind=domain_contribution) подмешивает свой primary
    # артефакт в optional-контекст автоматически. Новый домен → попадает
    # в ТЗ без правки этого файла.
    collect_optional:
      from_active_domain_packs: true
produces:
  artifact: common.requirements_spec@1.0.0
context:
  max_tokens: 64000
planning:
  priority: 100
effects:
  readiness:
    set:
      - dimension: specification_drafted
        status: ready
summary: |
  Это финальная сборка коммерческого ТЗ. Тебе на вход поступают все аналитические
  артефакты (нормализованный запрос, цели, бизнес-обоснование, scope, стейкхолдеры,
  варианты архитектуры, доменные разделы). Твоя задача — собрать их в связный
  проектный документ для заказчика.

  ЭТО НЕ МЕТА-ДОКУМЕНТ ПРО ТЗ. Это сам проектный документ — текст, по которому
  заказчик принимает решение запустить проект, а реализационная команда — берёт
  его и работает. Нигде не пиши «система должна формировать структурированное
  ТЗ»; пиши, что делает РЕШЕНИЕ из бизнес-запроса.

  РАЗДЕЛЫ ТЗ. Базовый набор, который применим к большинству консалтинговых PoV.
  Включай те разделы, для которых в upstream-артефактах есть содержание; не
  заполняй раздел generic-шаблоном ради формальности.

  • Executive summary — короткий абзац: что за проект, какой эффект, рамки.
  • Понимание задачи — что нужно создать, цели системы, бизнес-процессы, на
    которые проект влияет, ожидаемые бизнес-метрики.
  • Текущая ситуация — контекст заказчика, ключевые проблемы, доступные источники
    и заинтересованные стороны.
  • Предлагаемое решение — границы PoV, компоненты системы, методология работ,
    разделение данных или процессов (если применимо).
  • Схема реализации — общая архитектура, поток данных или взаимодействий, этапы
    работ как упорядоченная последовательность с зависимостями и условиями
    перехода между ними.
  • Результат и критерии приёмки — поставляемые результаты и измеримые критерии
    приёмки; по каждому критерию — как именно он проверяется (формат ниже).
  • Бизнес-обоснование — качественный эффект и бизнес-метрики успеха.
  • Технические требования и ограничения — компактно из constraint_inventory и
    доменных артефактов.
  • Топология развёртывания — если активен deployment_topology: среды, сетевые
    контуры, размещение компонентов, цикл поставки. Заполняй структурированное
    поле `deployment_topology` (environments / network_zones / components /
    deployment_flow).
  • Реестр рисков — если активен project_risk_register: структурированный
    список с probability/impact/mitigation/trigger/owner. Заполняй поле
    `project_risks_detail`. Старое поле `risks` (плоский список) оставляй
    пустым, чтобы избежать дубля.
  • Оценка воздействия на ПДн (DPIA) — если активен privacy_impact_assessment:
    заполняй структурированное поле `privacy_impact` (pii_categories,
    processing_purposes, lawful_basis, controls, residual_risks и т.д.).
  • Глоссарий — если активен glossary_terms: заполняй массив `glossary`
    (объекты term/definition/category/synonyms). Идёт приложением в конце.
  • Следующие шаги.
  • Перспектива масштабирования — что в следующих этапах.

  ⚠️ СТРОГАЯ СХЕМА ВЛОЖЕННЫХ ОБЪЕКТОВ — ЧИТАЙ ВНИМАТЕЛЬНО. Перечисленные ниже поля
  валидируются ЖЁСТКО (additionalProperties: false): лишний ключ, пропущенный
  обязательный ключ ИЛИ НЕВЕРНЫЙ ТИП (объект вместо массива, массив строк вместо
  объекта) валит ВЕСЬ артефакт ТЗ целиком — а это самая дорогая задача пайплайна
  (десятки тысяч токенов). Не добавляй ключей вне перечня и не меняй тип поля.

  МАССИВЫ ОБЪЕКТОВ (каждый ЭЛЕМЕНТ — объект с обязательными ключами):
  • deployment_topology.environments[]: ОБЯЗАТЕЛЬНО `name`, `purpose`
    (опционально `scaling`).
  • deployment_topology.components[]: ОБЯЗАТЕЛЬНО `name`, `placement`,
    `responsibilities` (опционально `technology`). Поле `name` — это имя
    компонента (например «API-шлюз», «Хранилище показаний»); без него элемент
    невалиден.
  • deployment_topology.network_zones[]: массив строк.
  • project_risks_detail[]: ОБЯЗАТЕЛЬНО `title`, `category`, `description`,
    `probability`, `impact`, `mitigation`. При этом `probability` и `impact` —
    строго одно из значений: `low` | `medium` | `high` (не «низкая», не «средний»).
    Опционально `trigger`, `owner`.
  • glossary[]: ОБЯЗАТЕЛЬНО `term`, `definition` (опционально `category`,
    `synonyms`).
  • acceptance_criteria[]: ОБЯЗАТЕЛЬНО `criterion` (измеримое условие успеха) и
    `verification_method` (КАК именно проверяется: метод — демо / тест / документ
    / интервью — и чем/на каких данных). Опционально `fit_criterion` (точный
    порог) и `source` (из какой цели/требования вытекает).

  ДОМЕННЫЕ ПОЛЯ — ЭТО ОДИН ОБЪЕКТ (НЕ массив объектов и НЕ список строк!) со
  строго перечисленными ключами. Если значение ключа не помечено как «строка» —
  это МАССИВ СТРОК. Включай поле ТОЛЬКО если активен соответствующий domain pack:
  • frontend_requirements (frontend): ОБЯЗАТЕЛЬНО `user_roles`, `user_flows`,
    `screens`, `ux_constraints` (опц. `analytics_views`, `decision_support_needs`).
  • ml_requirements (ml): ОБЯЗАТЕЛЬНО `prediction_target` (строка),
    `prediction_horizon` (строка), `prediction_unit` (строка), `data_sources`,
    `model_outputs`, `evaluation_metrics`, `explainability_requirements`.
  • integration_model (integration): ОБЯЗАТЕЛЬНО `source_systems`,
    `delivery_pattern` (МАССИВ строк, НЕ строка), `refresh_model` (строка),
    `target_surfaces`, `operating_roles` (опц. `dependency_risks`).
  • security_constraints_detail (security): ОБЯЗАТЕЛЬНО `deployment_constraints`,
    `privacy_constraints`, `access_control_constraints`, `allowed_ai_usage`,
    `mandatory_controls` (опц. `compliance_risks`).
  • privacy_impact (DPIA): ключи `pii_categories`, `processing_purposes`,
    `lawful_basis` (строка), `data_minimization`, `controls`,
    `data_subject_rights` (строка), `cross_border_transfers` (строка),
    `retention_policy` (строка), `residual_risks`. НЕ выдумывай других ключей
    (`data_categories`, `minimization`, `pseudonymization`, `residency`,
    `retention` — НЕВАЛИДНЫ, валят артефакт).

  Если по конкретному элементу / полю данных нет — НЕ вставляй заглушку с пустыми
  полями и НЕ опускай обязательный ключ; лучше не включать необязательный элемент
  целиком. Перед финализацией перепроверь КАЖДОЕ поле из этого блока: верный ТИП
  (объект vs массив) + все обязательные ключи на месте.

  ДОМЕННЫЕ ВКЛАДЫ. Если для проекта активны domain packs, их разделы
  подмешиваются автоматически (frontend → требования к интерфейсу; ml →
  ML-задача и данные; integration → интеграции; security → ИБ и комплаенс).
  Включай их СОДЕРЖАНИЕ, а не плейсхолдеры; если содержания нет — раздел
  опускай.

  ПРАВИЛА КАЧЕСТВА.

  • СОДЕРЖАТЕЛЬНО, А НЕ МЕТА. «Система должна формировать структурированное ТЗ» —
    запрещено. Пиши, что делает РЕШЕНИЕ в этом конкретном проекте.
  • ЦИТИРУЙ КОНКРЕТИКУ ИЗ ЗАПРОСА. Имена систем, числа, роли — те, которые реально
    упоминаются. Не вставляй имена, не упомянутые в запросе.
  • НЕ ТЕРЯЙ ФАКТУРУ. Конкретику из upstream, относящуюся к решению (имена систем,
    числа, требования, риски, эффект), переноси в финальный ТЗ полностью.
  • НЕ РАЗМЫВАЙ. «Допустимы выгрузки или API» — формулировка-уход. Лучше:
    «PoV работает на разовых обезличенных CSV-выгрузках; регулярная интеграция —
    следующий этап».
  • НЕ ВЫДУМЫВАЙ. Если данных в upstream нет — раздел компактный, явно отмечен в
    reasoning.
  • НЕ ПОДГОНЯЙ ПОД ЧУЖОЙ КЕЙС. Не вставляй компоненты (ML-модель, BI-дашборд,
    LLM-блок и т.п.), если в данном бизнес-запросе их нет. Структура документа гибкая.
  • СОГЛАСУЙ ГРАНИЦЫ С ДОМЕННЫМИ ТРЕБОВАНИЯМИ. (Темы вне документа — стоимость,
    сроки, юридическое — см. <out_of_scope> в системном промпте.) Документ не должен противоречить
    сам себе. Если доменный раздел (frontend_requirements, integration_model,
    ml_requirements и т.п.) ТРЕБУЕТ некий функционал — НЕ помещай этот же
    функционал в scope_out как исключённый. Перед финализацией сверь scope_in /
    scope_out с тем, что реально требуют доменные разделы: конфликт «исключено
    в scope_out vs требуется в доменном разделе» — это критический дефект, ТЗ с
    ним заказчик не примет. Спорный функционал помещай в ОДНУ сторону (in или out)
    с явной оговоркой, а не в обе сразу. Пример дефекта: scope_out исключает
    «уведомления жителям (SMS/push)», а frontend_requirements требует
    «напоминание за 3 дня до срока по SMS/push» — так нельзя.
  • НЕ ФИКСИРУЙ КОНКРЕТНУЮ ИНФРАСТРУКТУРУ И ЧИСЛА БЕЗ ИСТОЧНИКА. Конкретный
    облачный провайдер (Yandex Cloud, AWS, VK Cloud, конкретные регионы и
    managed-сервисы), точные количества (число лицевых счетов / пользователей /
    документов) — указывай ТОЛЬКО если они есть в бизнес-запросе или
    upstream-артефактах. Иначе: нейтральная формулировка
    («managed-сервисы облачного провайдера с ЦОД в РФ, например …»; «пилотная
    группа порядка N — требует подтверждения») + перенеси предположение в
    `assumptions`. Выдуманная конкретика — это галлюцинация, ревью её валит.
  • БЕЗ MARKDOWN ВНУТРИ СТРОКОВЫХ ЗНАЧЕНИЙ. Значения полей — чистый текст без
    разметки. НИКОГДА не вставляй ```json, ``` или другие code-fence-обёртки и не
    оборачивай фрагменты строки в markdown-блоки внутри значения поля. Это
    артефакт генерации, он портит документ (пример дефекта: «…сохраняются
    локально```json\nв Redis…»).

  ОЧЕНЬ ВАЖНО — ФОРМАТ КОНТЕНТА ПОЛЕЙ СХЕМЫ.

  Артефакт — это документ для человека, не сериализованный JSON. Большинство полей
  схемы — массивы строк, но это НЕ значит, что каждая строка = телеграфный bullet.
  Качественный документ — это смесь форматов.

  • ПРОЗА (текстовые поля схемы: executive_summary, business_context, business_goal,
    prediction_target, prediction_horizon, prediction_unit, refresh_model): пиши
    абзацами по 3-7 предложений со связной логикой, как раздел инвестиционного
    меморандума. Никаких однострочных лозунгов и никаких bullet'ов внутри одного
    строкового поля.

  • БОГАТЫЕ ПУНКТЫ СПИСКА (массивы success_criteria, functional_requirements,
    non_functional_requirements, risks, assumptions, alternatives_considered,
    target_outcomes, delivery_artifacts,
    integration_requirements, security_requirements, deployment_requirements,
    stakeholders, open_questions): каждый item — НЕ короткая фраза,
    а развёрнутая формулировка из 1-3 предложений: с контекстом, с цифрами, с
    обоснованием. Пользователь должен прочесть одну строку и понять суть.

    Плохой пример: «Снижение оттока на 7 п.п.»
    Хороший пример: «Снижение годового оттока розничного персонала на 7 процентных
    пунктов — основной экономический эффект, на который рассчитан проект; цифра
    подтверждается на валидационной выборке заказчика и переводится в денежный
    эквивалент через стандартную стоимость замены сотрудника.»

  • USER_STORIES — ЭТО МАССИВ ОБЪЕКТОВ, НЕ СТРОК. Каждый элемент:
    {actor, goal, steps[]}, где actor — роль; goal — что роль хочет получить;
    steps — упорядоченные шаги (массив строк, по одному действию на шаг). Это
    единственный объектный массив среди списков ТЗ — НЕ пиши его плоскими
    строками вида «Как X, я хочу Y». Пример одного элемента:
    {"actor": "HR-аналитик", "goal": "увидеть прогноз оттока по подразделению",
    "steps": ["Открывает дашборд", "Выбирает подразделение и период",
    "Видит список сотрудников с риском и факторами"]}.

  • PHASED_PLAN — ТОЖЕ МАССИВ ОБЪЕКТОВ, НЕ СТРОК. Каждый элемент:
    {phase, objective, result, milestone, depends_on[]}, где phase — название
    фазы; objective — цель; result — проверяемый результат; milestone —
    контрольное СОБЫТИЕ (не дата); depends_on — массив строк «от чего зависит
    старт фазы». БЕЗ длительностей и дат — только порядок и зависимости. Пример:
    {"phase": "Фаза 0. Подготовка", "objective": "Согласовать выгрузку и
    правомерность обработки", "result": "Зафиксирована пилотная выборка",
    "milestone": "Получен допуск ИБ", "depends_on": []}.

  • ACCEPTANCE_CRITERIA — ТОЖЕ МАССИВ ОБЪЕКТОВ, НЕ СТРОК. Каждый элемент:
    {criterion, verification_method, fit_criterion?, source?}. criterion —
    измеримое условие успеха; verification_method — КАК именно проверяется (метод:
    демо / тест / документ / интервью, и чем/на каких данных). Не пиши критерий
    плоской строкой без метода проверки. Пример:
    {"criterion": "Релевантность подсказки не ниже 80%", "verification_method":
    "Оценка аналитиком качества на выборке из ≥100 реальных диалогов по рубрике
    релевантности (релевантно/частично/нерелевантно)", "fit_criterion":
    "доля «релевантно» ≥ 80%", "source": "цель: ускорить адаптацию новичков"}.

  • РИСКИ — формат «Категория. Описание. Митигация:». Пример:
    «Качество данных. Низкая полнота НСИ в источниках приведёт к падению
    предсказательной способности модели. Митигация: ранний EDA на pilot-выгрузке,
    промежуточный отчёт заказчику с честной оценкой пригодности данных.»

  • АЛЬТЕРНАТИВЫ — формат «Название. Суть. Почему отвергнуто (или: при каких условиях
    стоит вернуться):». Не «вариант A / вариант B» в один присест.

  • СТЕЙКХОЛДЕРЫ — формат «Роль. Интерес. Влияние. Что делает в проекте.»

  ПРОВЕРКА. Когда закончишь — мысленно прочитай документ глазами клиента. Если он
  читается как «набор фраз через дефис» или как 10 одинаковых bullet-листов один за
  другим — переписывай items длиннее и связнее. Хороший ТЗ — это связный текст с
  ритмом «абзац прозы → таблица параметров → подробный список с обоснованиями»,
  а не «список → список → список».

  Объём — столько, сколько нужно для качественной передачи проекта. Краткость
  лучше воды; полнота лучше пропуска критичных тем.

  Не путай ФОРМАТ ТЗ (структура документа) и СОДЕРЖАНИЕ (что в нём написано).
  Структура — гибкая под природу проекта; содержание — извлекается из upstream-
  артефактов. Если в upstream-артефактах пробелы — отрази это в разделе
  open_questions / assumptions, не подменяй пробел общими словами.

  confidence 0.75-0.9 при качественных upstream-артефактах.
