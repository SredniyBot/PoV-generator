kind: task_template
id: ml.data_landscape_assessment
version: 1.0.0
title: Оценить данные для аналитики и ML
type: leaf
status: active
domain: ml
executor: llm
requires:
  state: [business_request]
  artifacts:
    required: [common.normalized_request@1.0.0]
produces:
  artifact: ml.data_landscape_assessment@1.0.0
context:
  include: [artifact.common.normalized_request]
  max_tokens: 24000
planning:
  priority: 730
effects:
  readiness:
    set:
      - dimension: data_feasibility_assessed
        status: ready
framework:
  summary: Оцени источники данных, качество, признаки, доступность, ограничения и риски реализуемости ML-задачи.
