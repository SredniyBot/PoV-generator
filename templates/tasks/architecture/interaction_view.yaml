kind: task_template
id: architecture.interaction_view
version: 1.0.0
title: Описать потоки взаимодействия
type: leaf
status: active
domain: architecture
executor: llm
methodology: minimal
requires:
  state: [business_request]
  artifacts:
    required:
      - architecture.system_context_definition@1.0.0
      - architecture.component_decomposition@1.0.0
    optional:
      - common.deployment_topology@1.0.0
      - integration.integration_operating_model@1.0.0
produces:
  artifact: architecture.interaction_view@1.0.0
context:
  include:
    - artifact.architecture.system_context_definition
    - artifact.architecture.component_decomposition
    - artifact.common.deployment_topology
    - artifact.integration.integration_operating_model
  max_tokens: 18000
planning:
  priority: 460
effects:
  readiness:
    set:
      - dimension: interactions_mapped
        status: ready
summary: |
  Опиши ключевые потоки взаимодействия между компонентами системы и
  внешними участниками. Это раздел про то, КАК работает система в
  динамике: какие сценарии происходят, кто кого вызывает, что передаётся.
  Опирайся на system_context_definition (внешние системы) и
  component_decomposition (внутренние компоненты).

  Структура:

  ▸ SUMMARY (опционально) — 1-3 предложения о ключевых сценариях
    взаимодействия и природе протоколов (синхронные REST / асинхронные
    события / батч-обработка).

  ▸ FLOWS — основные сценарии взаимодействия. Для каждого:
    — `name` — короткое имя сценария (например «Подача нового запроса»,
      «Ежедневное обновление справочников»)
    — `trigger` — что запускает сценарий (действие пользователя, расписание,
      внешнее событие)
    — `participants` — список участников (компоненты + внешние системы) в
      порядке появления в сценарии
    — `steps` — массив шагов. Каждый шаг — одна строка с описанием
      действия (например «API Gateway валидирует запрос и кладёт в Kafka»)

  ▸ MERMAID_SEQUENCE_DIAGRAM — диаграмма ключевого сценария в синтаксисе
    Mermaid. Это может быть либо `sequenceDiagram` (для строгих
    последовательностей), либо `flowchart TD` (для разветвлённых процессов
    с условиями). Зафиксируй тип через поле `diagram_kind`. Пример
    sequenceDiagram:

    ```
    sequenceDiagram
        User->>Gateway: POST /request
        Gateway->>Worker: enqueue
        Worker->>Store: save
        Worker-->>Gateway: ack
        Gateway-->>User: 202 Accepted
    ```

    КАК УПАКОВАТЬ ДИАГРАММУ В JSON: каждое объявление и каждая стрелка-сообщение —
    отдельная строка, разделённая `\n`. В JSON-значении это выглядит как:

    `"mermaid_sequence_diagram": "sequenceDiagram\nUser->>Gateway: POST /request\nGateway->>Worker: enqueue\nWorker->>Store: save\nWorker-->>Gateway: ack\nGateway-->>User: 202 Accepted"`

    Без `\n` диаграмма не отрендерится — рендерер ничего не угадывает.

  ▸ DIAGRAM_KIND (опционально) — `sequence` или `flowchart`. Если не
    указано — считается `sequence`.

  ▸ DATA_CONTRACTS (опционально) — формат данных, которыми обмениваются
    компоненты. Для каждой пары:
    — `from`, `to` — какие компоненты
    — `payload` — что передаётся (одной строкой)
    — `format` (опционально) — JSON / protobuf / CSV / event

  ▸ FAILURE_MODES (опционально) — что может пойти не так в сценарии и
    какое поведение системы ожидается (timeout / retry / circuit breaker /
    отказ с понятным сообщением). Каждый — одной строкой.

  Правила:
  • Не выдумывай флоу, которых не следует из контекста или компонентов.
  • В диаграмме используй те же имена узлов, что в component_decomposition.
  • Если deployment_topology доступен — учитывай размещение компонентов
    в зонах при описании сетевых вызовов.

  confidence 0.65-0.85.
