kind: artifact_contract
id: implementation.component_build_spec
version: 1.0.0
title: Спека сборки компонента
# Структурная схema — в application/artifact_contracts.py. Самодостаточный вход
# для harness-задачи реализации одного компонента.
unstructured: true
schema:
  type: object
  additionalProperties: true
