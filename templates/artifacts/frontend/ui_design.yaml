kind: artifact_contract
id: frontend.ui_design
version: 1.0.0
title: UI/UX дизайн
# Структурный артефакт (реальная схема — в application/artifact_contracts.py, как
# у component_model): принципы, цветовая палитра, типографика, экраны, навигация,
# формы, компоненты и подход к взаимодействию с API. Производится на гейте
# «Архитектура» доменным вкладом frontend-пакета и входит в design_document.
unstructured: true
schema:
  type: object
  additionalProperties: true
