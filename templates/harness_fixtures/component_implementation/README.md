# Компонент (демо-реализация)

Детерминированная фикстура harness-узла реализации компонента: показывает форму
выхода (исходный код как бандл). На stub-рантайме реальный агент не запускается —
возвращается этот набор файлов.
