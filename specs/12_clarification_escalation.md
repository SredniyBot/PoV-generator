# Решения и checkpoint-сессии

> **Статус:** v3.1 · действует · 2026-05-17
>
> История: v2.2 (ClarificationRequest, ClarificationService, отдельные «Вопросы»)
> полностью убрана. Старая модель заменена единой сущностью `Decision`. Код
> `clarification_service.py`, таблицы `clarification_requests/_candidates/_events`,
> UI ClarificationsPage / DecisionLogPage — удалены.

Документ описывает, как пользователь участвует в принятии решений по проекту через **реестр решений** + pre-flight checkpoint. Это единственный механизм участия — отдельной концепции «вопросов» в системе больше нет.

---

## Модель: реестр решений + checkpoint

### Что было неправильно в старой модели

Прежняя модель решала задачу «как фильтровать вопросы от LLM по уровню вовлечения пользователя». Это казалось правильным, но оказалось неправильным фундаментально: **подсистема фильтровала пустоту**.

LLM в процессе генерации артефакта принимает сотни решений. Из них в `ClarificationCandidate` попадали только те, где она явно застряла (низкая уверенность по валидатору или сработавшее методологическое правило). Остальное — десятки и сотни выборов — принималось молча и попадало в финальный артефакт.

Поэтому пользователь, выбравший режим вовлечения «эксперт», ожидал увидеть все технические решения, а получал только те, в которых LLM сама сомневалась. Это рассогласование между обещанием режима и его реальной работой делало подсистему **операционно невидимой**.

### Истинная потребность пользователя

Пользователь хочет **делать проект PoV** с определённым уровнем вовлечения:
- Совсем не вникая (autopilot) — пусть LLM решает всё, я готов посмотреть результат и принять или попросить переделать.
- На уровне бизнес-решений (balanced) — я отвечаю за «что делаем», техника на LLM.
- На уровне архитектуры (control) — я в курсе по бизнесу и архитектуре, детали на LLM.
- Глубоко (expert) — я хочу делать или хотя бы видеть все решения, включая исполнительские.

Это значит: **на выбранном уровне (и выше) пользователь хочет получать все решения** — не только те, в которых LLM не уверена. Под выбранным уровнем — решения принимаются автоматически, но пользователь хочет иметь возможность их **посмотреть постфактум** и при необходимости переиграть.

### Концепция: реестр решений (decision ledger)

Решение (`Decision`) — первоклассная сущность системы. У каждой задачи, генерирующей артефакт, появляется **два прохода**:

1. **Pre-flight planning.** Перед основной генерацией LLM делает отдельный проход: «перечисли решения, которые ты собираешься принять для этой задачи. Для каждого — предложенный выбор, альтернативы, обоснование, уровень (бизнес/архитектура/детали по критериям), уверенность».
2. **Checkpoint пользователю.** Список планируемых решений фильтруется по уровню режима (кумулятивно). Если оставшихся ноль (autopilot или все ниже уровня) — checkpoint пропускается. Иначе — пользователь видит сессию: подтверждает дефолты, выбирает альтернативы, оставляет свободный ответ, или явно откладывает (тогда применится дефолт, но пометка останется).
3. **Генерация с зафиксированными решениями.** Основной LLM-вызов получает решения как жёсткие ограничения и собирает артефакт. Решения, возникшие неожиданно в процессе генерации, дописываются в реестр без checkpoint.

**Реестр ведётся всегда**, во всех режимах, включая autopilot. Прозрачность универсальна. В autopilot отсутствует checkpoint, но не отсутствует видимость постфактум.

### Классификация уровня — универсальные критерии, не справочник типов

Уровень решения определяется **универсальными критериями**, применимыми к любому проекту любого домена. Никакого предопределённого справочника типов решений нет (любая попытка такого справочника обречена — каждый проект уникален).

Критерии формализованы в `docs/decision_level_criteria.md` и встроены в промпт планирования. LLM применяет критерии к каждому возникшему решению, объясняет выбор уровня, пользователь может переклассифицировать.

### Режимы — кумулятивные ступеньки на уровнях

| Режим | Уровни в checkpoint | Поведение |
|---|---|---|
| `autopilot` | (никаких) | реестр ведётся, checkpoint не показывается; рискованные решения подсвечены при постфактум-просмотре |
| `balanced` | business | пользователь участвует в бизнес-решениях, остальное автоматически |
| `control` | business + architecture | пользователь в курсе бизнеса и архитектуры |
| `expert` | business + architecture + detail | пользователь видит и может править все решения |

Это качественная разница с v2.2, где `balanced` и `control` были идентичны. Теперь различие поведенческое и понятное.

### Сценарии взаимодействия с реестром

Реестр — это инструмент, у которого должны быть разные стили работы (это ортогонально режиму):
- **Сессия** — последовательное прохождение всех ожидающих ответа решений одним потоком.
- **Список** — таблица с фильтрами (уровень, статус, источник, низкая уверенность), действия на конкретном элементе.
- **Массовое подтверждение дефолтов** — «применить все предложенные дефолты на моём уровне» одной кнопкой.
- **Postfactum-просмотр** — фильтр «решено системой автоматически», возможность переиграть любое.

### Override и каскад

Любое решение в реестре можно переиграть — независимо от режима, независимо от того, было ли оно показано в checkpoint или принято автоматически. При override:
- Затронутые артефакты помечаются устаревшими.
- Решения, зависевшие от изменённого, помечаются «требуют пересмотра».
- Перед применением показывается preview масштаба пересчёта.

В первой реализации (v3.0) — override `read-write`, каскадный пересчёт может быть отложен до v3.1 (read-only метка устаревания + ручная регенерация).

### Что становится с `ClarificationRequest` v2.2

Не удаляется. Переходит в роль **страховки**: если pre-flight планирование не предусмотрело некое решение, и в стадии генерации/валидации возникла блокирующая неопределённость — `ClarificationRequest` создаётся как раньше. Внутри он связывается с записью в реестре (как «emergent + reactive_validation»).

### Что меняется для эмиттеров

`validation_service`, `methodology_rules`, `quality_gate` — продолжают эмитировать `ClarificationCandidate` как раньше, но теперь это не основной поток, а fallback. Основной поток — pre-flight в `ExecutionService` до основной генерации.

### Инварианты v3.0

| ID | Правило |
|---|---|
| CE15 | Реестр решений ведётся всегда, во всех режимах, включая autopilot. |
| CE16 | Уровень решения определяется универсальными критериями (`docs/decision_level_criteria.md`), не справочником типов. |
| CE17 | Режимы — кумулятивные ступеньки на уровнях. Expert включает все уровни balanced и control. |
| CE18 | Pre-flight планирование выполняется перед генерацией артефакта, не вместо неё. Checkpoint фильтрует план по режиму. |
| CE19 | Решение, попавшее в реестр, имеет: choice, alternatives, rationale, level, level_rationale, confidence, source. Без любого из этих полей запись считается неполной. |
| CE20 | Override решения постфактум возможен в любом режиме, в любое время, по любому решению. |

### Соотношение со старыми инвариантами

| Старый | Статус | Заменён на |
|---|---|---|
| CE1–CE10 | Действует | — |
| CE11 (LLM формирует description/options/visibility/role) | Сужается до fallback-пути | В основном потоке формат решения шире, см. CE19 |
| CE12 (visibility — единственная ось ask/assume) | **Отменяется** | CE17 (режимы по уровням) + CE15 (универсальная прозрачность) |
| CE13 (право пересмотра универсально) | Действует | Усилено CE20 |
| CE14 (visibility обязательна) | Сужается до fallback-пути | В основном потоке уровень из CE16 |
